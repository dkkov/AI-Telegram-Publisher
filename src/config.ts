// «База знаний» бота — голос канала и правила стиля.
// Это фирменный стиль фитнес-канала: меняй CHANNEL_STYLE под свою нишу.

/** Читаем переменную окружения, кидаем понятную ошибку, если её нет. */
export function requireEnv(name: string): string {
  const value = process.env[name];
  if (!value) {
    throw new Error(`Не задана переменная окружения ${name}`);
  }
  return value;
}

/** Необязательная переменная с дефолтом. */
export function optionalEnv(name: string, fallback: string): string {
  return process.env[name]?.trim() || fallback;
}

/** Лимит бесплатных постов на пользователя. */
export const FREE_POST_LIMIT = Number(optionalEnv('FREE_POST_LIMIT', '2'));

/** Максимальная длина поста в символах. */
export const MAX_POST_LENGTH = 900;

/** Модель Gemini. Убираем возможный префикс "models/" и лишние пробелы. */
export const GEMINI_MODEL = optionalEnv('GEMINI_MODEL', 'gemini-2.5-flash')
  .trim()
  .replace(/^models\//i, '');

/**
 * Правила стиля фитнес-канала. Отдаём агентам в системном промпте.
 * 5–10 конкретных пунктов: тон, длина, эмодзи, формат, запреты.
 */
export const CHANNEL_STYLE = `
Ты — редактор Telegram-канала про фитнес и здоровый образ жизни.
Голос канала: энергичный, мотивирующий, но дружелюбный «на равных» — без снобизма и давления.
Правила стиля:
1. Тон бодрый и поддерживающий: обращайся к читателю на «ты», подбадривай.
2. Пиши по делу, с практической пользой: конкретные приёмы, цифры, примеры.
3. Длина поста — до ${MAX_POST_LENGTH} символов. Короткие абзацы по 1–3 предложения.
4. Эмодзи — умеренно, 3–6 штук по смыслу (💪🔥🥗🏃‍♀️🧘). Не в каждой строке.
5. Структура строго: цепляющий ХУК → ТЕЛО с пользой → ПРИЗЫВ к действию → 3–5 ХЭШТЕГОВ.
6. Хэштеги на русском/английском по теме, в конце, через пробел (#фитнес #питание ...).
7. Опирайся на факты из ресёрча, не выдумывай исследований и цифр.
8. Обязателен мягкий дисклеймер, если тема касается здоровья/нагрузок:
   напомни, что при проблемах со здоровьем стоит консультироваться со специалистом.
Запретные темы (не пиши и мягко откажись): рецептурные препараты и дозировки,
анаболические стероиды, экстремальные голодания, диагнозы и «лечение» болезней,
опасные челленджи. Это не медицинский совет.
`.trim();

/** Приветствие при /start. */
export const WELCOME_MESSAGE = [
  '💪 Привет! Я бот фитнес-канала.',
  '',
  'Пришли мне тему — и я соберу по ней свежие факты и напишу готовый пост с обложкой.',
  '',
  'Например: «как начать бегать с нуля», «сколько белка нужно в день», «разминка перед тренировкой».',
  '',
  'А ещё я принимаю правки: после поста напиши «короче», «добавь пример», «сделай проще» — и я перепишу.',
].join('\n');

/** Сообщение при достижении лимита. */
export const LIMIT_MESSAGE = [
  '🙌 Ты использовал все бесплатные посты.',
  '',
  `Бесплатно доступно ${FREE_POST_LIMIT} постов на пользователя.`,
  'Дальше — по подписке. Оформление подписки скоро добавим 🙂',
].join('\n');
